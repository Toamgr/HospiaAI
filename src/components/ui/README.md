# Shared UI Components

Shared presentational components should move here as the app is extracted from `App.jsx`.

Examples:

- `Button`
- `Card`
- `Header`
- `Metric`
- `Alert`
- `Field`
- `TextArea`

Keep behavior in feature modules or hooks; keep this folder focused on reusable interface primitives.
