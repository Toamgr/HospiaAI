# Hooks

Reusable React hooks should live here as the large app shell is decomposed.

Good first candidates:

- `useAuth`
- `usePersistentState`
- `useNotifications`
- `useShiftBrain`
- `useAcademyProgress`
