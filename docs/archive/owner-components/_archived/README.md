# owner/_archived

**Dead code.** These files are no longer imported anywhere in the application.

They have been superseded by purpose-built replacements in `../wip/` (placeholder shells) or by a complete redesign (`OperationalPulse.jsx`).

Do not import from this directory. Do not re-export from this directory. If a page here needs to come back, rebuild it from scratch using the current data contracts.

## Files (superseded)

| File | Superseded by |
|---|---|
| `BusinessMRI.jsx` | `../wip/BusinessMRI.jsx` |
| `ExecutiveOverview.jsx` | `../wip/ExecutiveOverview.jsx` |
| `ProfitLeaks.jsx` | `../wip/ProfitLeaks.jsx` |
| `StrategicRecommendations.jsx` | `../wip/StrategicRecommendations.jsx` |
| `WeeklySummary.jsx` | `../wip/WeeklySummary.jsx` |

## What is NOT here

Pages with working code that are feature-flagged off live in `../legacy/`.
Pages under active development live in `../wip/`.
